# Tesseract OCR dependencies (Python 3.8.10 compatible)
# Faster OCR solution - requires system tesseract-ocr package
# Best for production environments with admin rights

# Install with: pip install -r requirements-ocr-tesseract.txt

pytesseract>=0.3.8,<1.0.0
pdf2image>=1.14.0,<2.0.0

# Additional system requirement:
# Ubuntu/Debian: sudo apt-get install tesseract-ocr
# macOS: brew install tesseract
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki
