# EasyOCR OCR dependencies (Python 3.8.10 compatible)
# Pure Python solution - no system installation required
# Recommended if you don't have admin/sudo rights

# Install with: pip install -r requirements-ocr-easyocr.txt

easyocr>=1.4.0,<1.8.0
pdf2image>=1.14.0,<2.0.0
torch>=1.9.0,<2.0.0
torchvision>=0.10.0,<0.14.0
opencv-python-headless>=4.5.0,<5.0.0

# Note: First run will download ~100MB language model
# EasyOCR works without system-level Tesseract installation
